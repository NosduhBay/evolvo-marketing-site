import 'package:flutter/material.dart';

class AppColors {
  // A clean, pure white for backgrounds
  static const Color background = Colors.white;
  
  // A very dark grey for primary text and elements, softer than pure black
  static const Color primaryText = Color(0xFF212121); 

  // A slightly lighter grey for secondary text or subtle borders
  static const Color secondaryText = Color(0xFF757575); 

  // A light grey for container backgrounds or dividers
  static const Color lightGrey = Color(0xFFFAFAFA);

  // A strong, dark color for buttons and accents
  static const Color accent = Colors.brown;
}